apiVersion: v1
kind: Service
metadata:
  name: carbon-kube-metrics
  labels:
    app: carbon-kube
spec:
  selector:
    app: carbon-kube-mutator
  ports:
    - name: http
      port: 9090
      targetPort: 9090
