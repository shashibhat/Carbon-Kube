{{- if .Values.metrics.serviceMonitor.enabled }}
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: carbon-kube-metrics
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: carbon-kube
  endpoints:
    - port: http
      interval: 30s
{{- end }}